{% extends 'admin/base.html.twig' %}

{% block body_content %}

    {{ parent() }}

    <h1 class="fs-2 d-flex justify-content-between">
		<span>{{ invoiceReceived.number }}</span>
		<span>{{ invoiceReceived.supplier.ciName }}</span>
	</h1>

    <div class="card">

        <div class="card-body">

            <div class="table-responsive">

                <table class="table table-striped table-hover table-nowrap align-middle mb-0">

                    <tbody>
                        <tr>
                            <th>{{ 'label.id'|trans({}, 'invoice_received')|lower }}</th>
                            <td>{{ invoiceReceived.id }}</td>
                        </tr>
                        <tr>
                            <th>{{ 'label.number'|trans({}, 'invoice_received')|lower }}</th>
                            <td>{{ invoiceReceived.number }}</td>
                        </tr>
                        <tr>
                            <th>{{ 'label.status'|trans({}, 'invoice_status')|lower }}</th>
                            <td>
                                {% set status = invoiceReceived.status.id %}
                                {% if status == '' %}
                                {{ 'status.select'|trans|lower }}
                                {% endif %}
                                {% if status == 0 %}
                                <span class="badge bg-danger">{{ 'status.unpublish'|trans|lower }}</span>
                                {% endif %}
                                {% if status == 1 %}
                                <span class="badge bg-success">{{ 'status.publish'|trans|lower }}</span>
                                {% endif %}
                            </td>
                        </tr>
                    </tbody>

                </table>

            </div>

        </div>

    </div>

    <div class="row">

		{% if dateLogs %}

			<div class="col-12">

				<div class="card">

                    <div class="card-header align-items-center d-flex">
                        <h4 class="card-title mb-0 flex-grow-1">{{ 'fieldset.history_of_modifications'|trans }}</h4>
                        <button class="btn btn-sm btn-info" type="button" data-bs-toggle="collapse" data-bs-target="#collapseInvoiceHistoryOfModifications" aria-expanded="false" aria-controls="collapseInvoiceHistoryOfModifications"><i class="mdi mdi-information-outline"></i></button>
                    </div>

					<div class="card-body collapse" id="collapseInvoiceHistoryOfModifications">

						<div class="table-responsive">

							<table class="table table-striped table-hover table-nowrap align-middle mb-0">

								<tbody>

									{% for dateLog in dateLogs %}

										<tr>

											{% set date = dateLog.updatedAt|date('Y-m-d H:i:s') %}

											{% if date == '1000-01-01 00:00:00' %}

												<th>{{ 'label.created_at'|trans|lower }}</th>
												<td>{{ invoiceReceived.createdAt|date(app.request.attributes.get('user').userUserSettings and app.request.attributes.get('user').userUserSettings.dateTimeFormat ? app.request.attributes.get('user').userUserSettings.dateTimeFormat : 'Y-m-d H:i:s') }}</td>

											{% else %}

												<th>{{ 'label.updated_at'|trans|lower }}</th>
												<td>{{ date|date(app.request.attributes.get('user').userUserSettings and app.request.attributes.get('user').userUserSettings.dateTimeFormat ? app.request.attributes.get('user').userUserSettings.dateTimeFormat : 'Y-m-d H:i:s') }}</td>

											{% endif %}

											<td>{{ dateLog.user.userUserContact.firstname }}
												{{ dateLog.user.userUserContact.surname }}</td>

										</tr>

									{% endfor %}

								</tbody>

							</table>

						</div>

					</div>

				</div>

			</div>

		{% endif %}

	</div>
    
    <div class="d-flex justify-content-between">
        <div>
            <a href="{{ path('app_admin_invoice_received_edit', {'id': invoiceReceived.id}) }}" title="{{ 'action.edit_invoice_received'|trans({}, 'invoice_received') }}" class="btn btn-success">{{ 'action.edit_invoice_received'|trans({}, 'invoice_received') }}</a>
            <a href="{{ path('app_admin_invoice_received_index') }}" title="{{ 'action.back_to_list'|trans }}" class="btn btn-info">{{ 'action.back_to_list'|trans }}</a>
        </div>
        <div>
            {{ include('admin/invoice_received/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
