<form method="post" action="{{ path('app_admin_invoice_issued_settings_delete', {'id': invoice_issued_settings.id}) }}" onsubmit="return confirm('{{ 'alert.are_you_sure_you_want_to_delete_this_item'|trans }}');" class="d-inline-block float-end">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invoice_issued_settings.id) }}">
    <button class="btn btn-danger">{{ 'action.delete'|trans }}</button>
</form>
